"""Unit tests for the AI subsystem.

All tests use ``pytest`` with ``unittest.mock`` — no live network calls.

Modules:
    test_access_policy: Tests for the AI access-policy enforcement layer.
    test_adapter_factory: Tests for the AI adapter factory and provider switching.
    test_rag_context_provider: Tests for RAG context building and retrieval.
    test_tool_bridge: Tests for the AI tool-bridge interface.
"""
