# Motion-matching leaderboard

_Auto-generated by `scripts/run_leaderboard.m` (#4080)._
_Regenerated whenever a PR touches `motion_matching/`._

This file is the canonical cross-option comparison surface defined by
[`VISUALIZATION_SPEC.md` §"Comparison across options"](../shared/VISUALIZATION_SPEC.md).
Each PR that touches `motion_matching/` is expected to regenerate this
file by running `run_leaderboard.m` locally (MATLAB / Simulink are not
available on the hosted CI runners — see `.github/workflows/motion-matching-leaderboard.yml`
for the manual-step contract).

## Per-pair fit results

| swing_id | option    | solver | rmse_mm | work_J | wall_s | commit | timestamp | status        |
| -------- | --------- | ------ | ------- | ------ | ------ | ------ | --------- | ------------- |
| TW_ProV1 | fmincon   | n/a    | n/a     | n/a    | n/a    | -      | -         | pending #4074 |
| TW_ProV1 | surrogate | n/a    | n/a     | n/a    | n/a    | -      | -         | pending #4075 |
| TW_ProV1 | inverse   | n/a    | n/a     | n/a    | n/a    | -      | -         | pending #4076 |
| TW_ProV1 | hybrid    | n/a    | n/a     | n/a    | n/a    | -      | -         | pending #4077 |

## Cross-option grid (rmse_mm)

| trial \ option | fmincon         | surrogate       | inverse         | hybrid          |
| -------------- | --------------- | --------------- | --------------- | --------------- |
| TW_ProV1       | _pending #4074_ | _pending #4075_ | _pending #4076_ | _pending #4077_ |

## Notes

- `not yet available` cells indicate the optimizer entry point is not yet
  on the MATLAB path; see the linked option PRs (#4074, #4075, #4076, #4077).
- Per-pair `.mat` files are written next to this report as
  `<trial_id>_<option>.mat`.
- The grip-RMSE acceptance gate (#4080 AC3) is
  `max(rmse_mm) - min(rmse_mm) <= 5 mm`; document divergence in this
  file when the gate trips.
- This file regenerates on every PR that touches `motion_matching/`. To
  refresh locally:

  ```matlab
  cd src/engines/Simscape_Multibody_Models/3D_Golf_Model/matlab
  addpath(genpath('motion_matching'));
  run_leaderboard;        % defaults: TW_ProV1 × {fmincon,surrogate,inverse,hybrid}
  ```
