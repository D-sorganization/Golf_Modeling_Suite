# CI Requirements - Core dependencies only (excludes heavy modeling packages)
# This file is used in CI/CD to avoid long installation times
# For local development, use requirements.txt which includes all dependencies

# Core scientific computing libraries
# NOTE: numpy 2.x is now supported by main project (>=1.26.4,<3.0.0)
numpy==2.1.0
pandas==2.2.3
matplotlib==3.9.1
scipy==1.14.0

# Testing and development
# Security: pytest>=9.0.3 fixes CVE-2025-71176 (GHSA-6w46-j5rx-g56g)
pytest==9.0.3
pytest-cov==5.0.0
pytest-mock==3.14.0

# Code quality and formatting
# NOTE: ruff version aligned with main project pyproject.toml
ruff==0.14.10
# Security: black>=26.3.1 fixes CVE-2026-32274 (GHSA-3936-cmfr-pm3m)
black==26.3.1
mypy==1.10.0
pre-commit==4.0.0

# Configuration and utilities
pyyaml==6.0.1
# NOTE: pathlib2 removed - pathlib is built-in for Python 3.11+

# Type stubs for better type checking
types-PyYAML==6.0.12.12

# Shared module dependencies (required by src/shared/python)
defusedxml>=0.7.1
structlog>=24.0.0
colorama>=0.4.6

# Note: Modeling dependencies (pin, pin-pink, drake, qpsolvers) are excluded
# from CI to speed up builds. Install them locally using:
# pip install -r python/requirements.txt


