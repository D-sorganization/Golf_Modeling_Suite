# Docker

This directory contains the canonical root Dockerfile documentation.

- `Dockerfile` is the canonical default release/runtime/training image.
- `Dockerfile.heavy_test` is the dedicated heavy_test parity path.
- `Dockerfile.modular` is the supported opt-in modular profile path.
