# Agent Handoff — UpstreamDrift

Last updated: 2026-08-24

This file records current operational state, not history. Git and GitHub retain
history. Epic #8557 is the single proximal-to-distal completion authority.

## Repository Authority

- UpstreamDrift owns scientific sources, models, evidence registers, and the
  release bundle. AffineDrift is a generated, immutable, revision-pinned public
  projection. Tools owns reusable consumers; do not duplicate its solvers or
  edit `vendor/ud-tools` directly.
- The #8752 terminal reconciliation worktree is
  `C:\Users\diete\Repositories\UpstreamDrift-worktrees\8752-terminal-reconciliation`
  on `research/8752-terminal-reconciliation`. Reconciliation merge
  `c3471998d00a719a1b94607ce7e9902e267cc69a` merges terminal source commit
  `2fa6cf8861eeaf7ae111dd8dd18c4053a9f82e65` with remote-main snapshot
  `eb7076466152cbacd40a7f4d3fb9d92255d4ae43`. The current handoff commit is
  `SELF`. Full protected PR
  [#9046](https://github.com/D-sorganization/UpstreamDrift/pull/9046) is open,
  advances but intentionally does not close #8752, requires human review, and
  must not use auto-merge. #8800 remains an explicit #8752 completion blocker.
- The current computational paper is 239 pages, 1,870,344 bytes, and has
  SHA-256 `be85b7b62bba060a26ce3fea8355aa8b01dcf8c1b1ccf09304450898a4e5e78b`.
  All pages render; 194 URI links and 247 outline entries validate. The PDF is
  computational-release ready but not archival-ready because it remains
  untagged and contains Type 3 and unembedded font resources.

## Completed #8752 Terminal Evidence

- ControlTower completed the constitutive headline campaign on source
  `13146cdcece879e7156e06e2dca6626c1a54e045` at 2026-08-23T21:59:22Z with
  exit code 0. It executed 19 registered corners and retained 38 pathway
  outcomes. The isolated extraction contains 1,329 files.
- Terminal-record SHA-256 is
  `e8a7e53701217e4de875a370f7483172f3cfbfb167416b5133ba269b8fef689b`;
  archive SHA-256 is
  `c54e021eb5ad1e8270ee2a6b473c2cb6d9799583fd41c190b19df9581a6f6d1a`;
  manifest SHA-256 is
  `0465ee3e91a96b7724d8def0f0f415cfdd8ee7d5cf0b16f89dcbea696b9d144a`.
- Nine completed nonnominal shaft corners span 80–182 matched cells versus
  nominal 126/384; both grip-damping shaft corners remain retained failures.
  All 18 completed ground corners remain 0/384; high grip damping remains a
  retained ground failure.
- These are one-at-a-time engineering sensitivities of the matching set, not
  speed effects, probabilities, parameter interactions, equipment calibration,
  participant evidence, human mechanisms, or coaching recommendations.
- The reconciliation adds explicit normalized outcomes for claims 305–314 and
  executable numeric contracts for all quantitative #8752 claims. Generated
  reviewer-table candidates now reconcile deterministically instead of making
  the paper digest stale after claim-count changes.

## Claim and Release Authority

- The current authority covers 1,126/1,126 reviewed narrative candidates and
  313 material claims: 293 supported within their declared estimands, five
  inconclusive, 15 untested, and zero contradicted. A supported claim may report
  a null, adverse, or retained-failure result; it does not imply theory survival.
- Numeric authority covers 132 quantitative claims and 410/410 verified
  literals: 200 local JSON values, 146 explicitly registered but not
  independently recomputed values, seven protocol/notation values, and 57
  reported external values. One nondegenerate cross-engine comparison remains.
- Evidence integrity covers 2,333 references, 350 local artifacts, and 78
  external URLs. The deterministic release contains 653 artifacts. External
  review registers 56 canonical works; automated access is restricted for 42,
  but none is silently promoted beyond its registered scope.
- Momentum-transfer readiness has nine critical questions: eight answered or
  partly answered and MTQ-06 still definition/data gated. All have model and
  human test plans; human execution remains blocked by absent governed
  synchronized bilateral six-axis grip-wrench participant data.

## #8800 Structural Propagation Boundary

- The earlier ControlTower #8800 run is stopped and is only 93/830 checkpoints:
  shaft nominal 48/48, ground nominal 45/72, and 737 registered branches absent.
  `release_evidence=false`; no campaign process is running.
- Reconciliation regenerated the governed seven-corner plan against current
  source. New design SHA-256 is
  `d50568a8df8fecee2da4285458bc5373d41389924ba9730713224658f72c7d5a` and
  contract SHA-256 is
  `438bc0078358597d64893f71a73e64da29a9398cd23fea9e7e3b07522615c52e`.
  The deterministic plan-file SHA-256 is
  `59a6a0cbacffbf55ab1a4c3d6fa68bded66f4960a69402718db72c632743bbf7`.
  The prior partial checkpoints were created under different plan/source
  identities and must not be reused as release evidence or resumed without an
  explicit recovery/restart decision and exact checkpoint revalidation.
- WSL distributions remain stopped. Do not restart a campaign, mount/repair the
  ControlTower VHDX, change runner services, or substitute partial/synthetic
  evidence without explicit authority. A fresh #8800 execution should run on
  ControlTower under bounded resources after #8752 merges.

## External and Downstream Boundaries

- #9004 remains open because no qualifying governed participant trajectory
  dataset or held-out human outcome is registered. Tutorials, fixtures,
  Simscape exports, GolfDB labels, and launch-monitor records are not substitutes.
- #8556 remains externally blocked by absent governed synchronized bilateral
  six-axis grip-wrench participant data. Synthetic traces and paper-level curves
  must never substitute for human validation.
- AffineDrift publication projection remains downstream of the qualified,
  merged UpstreamDrift release. Pin the eventual squash/merge commit, never
  `/main/`, and do not project this moving reconciliation branch.
- Tools consumers must preserve UpstreamDrift evidence boundaries and immutable
  provider identity. Re-audit current Tools main before beginning the next child.

## Current Validation

- The complete changed scientific surface (38 files) passes serially; one native
  restart test skips locally because robotics Pinocchio is not installed.
- A separate 76-test propagation-plan/checkpoint subset passes. The focused
  #8752, claim-summary, and numeric-registration set passes 45 tests.
- Ruff check and format pass for all 70 Python files in the PR diff. Title-case,
  changed-file whitespace, and tracked-file size-budget checks pass.
- Claim audit, numeric audit, release review, evidence integrity, external-source
  review, momentum readiness, and computational open-release qualification pass.
- Computational qualification rendered all 239 PDF pages. Archival qualification
  intentionally remains false.
- The architecture gate exposed seven pre-existing #8752 orchestration/API
  violations. They are registered as narrow exceptions expiring 2026-09-30
  under open decomposition issue #8963; acceptance requires byte-equivalent
  evidence, registry, and plan hashes before removing them.
- The first protected pre-push attempt passed Ruff, title case, mypy, Bandit,
  and unit tests, then correctly stopped because Prettier rewrote the generated
  Simscape `.leaderboard_meta.json`. Its committed bytes are retained and the
  exact generated path is now excluded from formatter mutation.
- CI Standard on PR #9046 passed architecture, size, SPEC, DRY, error-handling,
  and suppression gates, then exposed 18 net-new unmarked scientific tests in
  three #8752 modules. Those modules now declare `pytest.mark.scientific`; the
  resulting source-identity change was propagated through the deterministic
  structural plan and release manifests. All 37 focused tests pass; the marker
  baseline shrank by 38 entries to 19,769 and the ratchet reports no drift.

## Immediate Next Actions

1. Push the suite-marker correction, then shepherd PR #9046 through protected
   CI and human review without bypasses or redundant reruns.
2. After merge, verify the merge commit is on remote `main`, update this handoff,
   then make an explicit #8800 recovery-versus-fresh-execution decision. Keep
   #8752 open until #8800 produces the registered evidence or is explicitly
   adjudicated under its own acceptance contract.

## Repository Rules

- Use TDD, DbC, DRY, and LoD. Edit canonical source, regenerate governed
  artifacts, retain failures, and use title case in document headings.
- Never force-push, admin-merge, bypass checks, add quarantine debt, or generalize
  model-conditional results to human or coaching authority.
- Distinguish energy transfer, momentum redistribution, joint work, constraint
  work, and clubhead speed. Preserve countermodels, falsifiers, uncertainty,
  identifiability limits, adverse cases, and unavailable states.
