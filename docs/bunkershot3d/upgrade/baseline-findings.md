# BunkerShot3D — baseline defect audit (2026-08-13)

Base: `origin/main` @ `973c9342c`. Worktree: `UpstreamDrift-worktrees/bunker-pro`,
branch `feat/bunkershot-pro-epic`.

Baseline test result: **2 failed, 138 passed, 3 skipped** (`pytest tests/bunkershot3d`).

Source size: ~1,530 LOC in `src/bunkershot3d/`, ~280 in `src/tools/bunker_shot_gui/`.

## P0 — the model does not model the thing

| #   | Finding                                                                                                                                                                                                                                                                                                                                                                                                                             | Evidence                                                                        |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------- |
| B1  | **There is no ball anywhere in the system.** A bunker shot is club → sand → ball. The package simulates a block pushing sand and never computes a ball launch. No launch speed/angle/spin output exists.                                                                                                                                                                                                                            | `grep -ri ball src/bunkershot3d` → 0 hits                                       |
| B2  | **LIGGGHTS deck contains no clubhead.** `_generate_input_deck` emits a sand box + gravity + insertion + run. Nothing strikes the sand. The "backend" cannot produce a bunker shot.                                                                                                                                                                                                                                                  | `backends/liggghts/driver.py:127-193`                                           |
| B3  | **Chrono wrench output is not the contact wrench.** `GetAppliedForce()` returns externally _applied_ loads, not the constraint/contact reaction. Reported force is ~0 regardless of sand.                                                                                                                                                                                                                                           | `backends/chrono/driver.py:204-208`                                             |
| B4  | **MuJoCo driver teleports the clubhead via `qpos` without setting `qvel`.** Contact impulses are solved against a body whose velocity never reflects the swing, so the sand never sees the 25 m/s impact.                                                                                                                                                                                                                           | `backends/mpm/driver.py:187-188`                                                |
| B5  | **MuJoCo contact wrench has no sign convention for geom order.** Verified empirically (sphere at rest on a plane, m·g = 41.888 N): `frame.T @ raw[:3]` returns the world force **on geom2's body** and matches `qfrc_constraint` exactly. So the transpose is right, but the force on the clubhead is the _negative_ whenever the clubhead is `geom1`. The code adds it unsigned in both cases, so oppositely-ordered pairs cancel. | `backends/mpm/driver.py:130-141`; probe in `_review/bunkershot-pro-2026-08-13/` |
| B5b | **The dominant moment on the clubhead is not computed at all.** `raw[3:]` from `mj_contactForce` is only the torsional/rolling friction couple _at the contact point_. The code sums those and calls it the torque. The `(r_contact − r_CoM) × F` moment arm term — the entire reason a wedge twists, digs, or resists opening — is absent. For club design this is the single most important output and it is missing.             | `backends/mpm/driver.py:141`                                                    |
| B6  | **`CoSimulator.step` returns a hard-coded wrench** `[10,0,5]` / `[0,1,0]` and the backend push/step lines are commented out. It is a stub presenting as a co-simulator.                                                                                                                                                                                                                                                             | `kinematics/coupling.py:116-141`                                                |
| B7  | **Dual-module identity split.** `calibration/calibrate_all.py` does `sys.path.insert(0, ...)` at import and imports `src.bunkershot3d.*`, so `src.bunkershot3d.exceptions.BackendNotImplementedError` and `bunkershot3d.exceptions.BackendNotImplementedError` are different classes. `except`/`pytest.raises` across the boundary fails. **This is one of the 2 baseline test failures.**                                          | `calibration/calibrate_all.py:27-34`                                            |

## P0 — the configured simulation is arithmetically impossible

| #   | Finding                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Evidence                                                               |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------- |
| B29 | **The canonical config contains no sand bed.** 50,000 grains at d=0.4 mm in a 0.4×0.3×0.1 m domain is a solid volume fraction of **1.4×10⁻⁴ (0.014 %)** — a settled bed **0.023 mm deep**, about 1/17th of one grain diameter. Every "bunker shot" run from `canonical.yaml` swings a club through an essentially empty box. A real 100 mm USGA bunker base at φ=0.60 needs **2.1×10⁸ grains**; even a 10 mm token bed needs **2.1×10⁷**. Resolved DEM at true grain size is therefore ~3 orders of magnitude beyond the configured count and ~6 days/shot of compute. | `calibration/configs/canonical.yaml`; arithmetic reproduced in session |
| B30 | **Chrono integrates at ~11,900× the stability limit.** It sets `dt = 1.0 / config.output_rate_hz` = 5×10⁻⁴ s as the _integrator_ step. The Rayleigh criterion for 0.2 mm quartz grains (G=29 GPa, ν=0.17, ρ=2650) gives t_R = 2.10×10⁻⁷ s, so the usual 0.2·t_R limit is **4.2×10⁻⁸ s**. Conflating the output sampling rate with the integration timestep is a category error; the run cannot be stable.                                                                                                                                                              | `backends/chrono/driver.py:176`                                        |

**Consequence:** resolved DEM cannot be the primary solver. This is not a gap to fill but a
constraint to design around — it forces the multi-fidelity architecture (fast analytic core

- optional high-fidelity reference), rather than "make the DEM backends work".

## Corrections to this audit

**B5 was real but LATENT, not active** — my original characterisation was wrong about when it
fires. MuJoCo orders a contact pair by **collider type, not geom id**. Verified directly: a box
declared first against a sphere declared second is reported as `geom1=sphere, geom2=box`,
regardless of declaration order; box-against-box preserves declaration order. Since the shipped
MJCF has a box clubhead and sphere grains, the clubhead was _always_ geom2, so the unsigned sum
happened to be correct for grain contacts. The defect activates for any box-vs-box or mesh pair —
wall contacts today, and W2's parametric wedge mesh (#8609) the moment it lands. Fixed in #8612
with an explicit sign, and the regression test forces the box-vs-box ordering rather than relying
on the accidental escape. Two-contact red proof: unsigned sum reported 94.1 N against a true
35.3 N.

**B32 (new, found while fixing B30): `canonical.yaml` numbers were parsing as strings.**
YAML 1.1 requires a signed exponent, so `1.0e7` is a _string_, not a float — silently coerced by
pydantic. Rewritten as `7.0e+10` / `4.0e-4`, with a test pinning `isinstance(..., float)`.

**B33 (new): `canonical.yaml` points at a `reference_swing.csv` that does not exist.**
Previously this fell through to the silent 5 m/s substitution (B10). It is now a loud
`TrajectoryUnavailableError` naming every candidate path tried.

## P1 — physics / numerics wrong

| #   | Finding                                                                                                                                                                                                                                                                                   | Evidence                                       |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------- |
| B8  | **Quaternion interpolation is component-wise `np.interp` + renormalise (nlerp), with no antipodal sign fixing.** A trajectory crossing a hemisphere interpolates through the wrong arc; near-antipodal pairs can produce a near-zero-norm quaternion. Must be SLERP with sign continuity. | `kinematics/trajectory.py:94-100`              |
| B9  | **Chrono ignores the trajectory entirely**: hard-coded `impact_velocity = 5.0`, 200 fixed steps, `dt = 1/output_rate_hz` used as the _integrator_ timestep (0.5 ms) — far above the Rayleigh-time limit for E=10 MPa, 0.4 mm grains. Silently unstable/wrong.                             | `backends/chrono/driver.py:176-196`            |
| B10 | **MuJoCo fallback also hard-codes `vel = 5.0` m/s** when no trajectory file resolves — a silent physical substitution, not an error.                                                                                                                                                      | `backends/mpm/driver.py:190-192`               |
| B11 | **LIGGGHTS: lognormal spec emitted as a Gaussian.** Config declares `diameter_sigma_log` (log-normal) but the deck writes `radius gaussian <mean> <sigma_log> ...`. Wrong distribution, and σ is interpreted in metres not log-space.                                                     | `backends/liggghts/driver.py:164-166`          |
| B12 | **LIGGGHTS `dt = 1.0e-5` hard-coded in two places** (deck writer and dump parser) with no Rayleigh/Hertz timestep criterion, and `total_steps` derived from a hard-coded 0.5 s that ignores `trajectory.duration`.                                                                        | `backends/liggghts/driver.py:119,123,221`      |
| B13 | **No timestep stability criterion anywhere.** No Rayleigh time, no CFL, no Hertzian contact-duration check. `docs/comparison.md` claims a 0.2 Rayleigh safety factor that is not implemented.                                                                                             | `docs/bunkershot3d/comparison.md:12` vs code   |
| B14 | **Angle of repose is a weak identifier used as a primary calibration.** Grid search over 9 friction values on a 200-grain sphere pile, clipped to [5°,70°], with magic fallbacks returning `30.0` in four different failure branches — silently fabricating a plausible answer.           | `calibration/angle_of_repose.py:87,96,114,125` |
| B15 | **Sphere-only DEM with no rolling friction.** Spherical grains without a rolling-resistance model cannot reach the shear strength of angular silica sand; friction identified this way is not transferable.                                                                               | all three backends                             |
| B16 | **MuJoCo grain init uses `rng.uniform` over the whole domain** — grains are initialised interpenetrating and explode on the first step; only the 500-step "settle" masks it. Chrono uses `np.linspace` z-layers, which stacks all grains in a single vertical line for large counts.      | `mpm/driver.py:68-71`, `chrono/driver.py:108`  |

## P2 — architecture / engineering quality

| #   | Finding                                                                                                                                                                                                                                                                                                                                           |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| B17 | **HDF5 schema is pathological**: one HDF5 _group per timestep_, each holding a 3-element dataset. 2 kHz × 0.5 s = 1000 groups × 2. Read path is `sorted(grp.keys())` on `f"t_{time:.6f}"` strings — lexicographic order breaks at t ≥ 10 s. Should be contiguous chunked arrays. `io/schema.py`                                                   |
| B18 | **No result schema version, no provenance, no config hash, no seed record, no environment capture.** Results are not reproducible or auditable.                                                                                                                                                                                                   |
| B19 | **`BunkerShotConfig` is a god object with 15 flat delegating accessors** added to satisfy LoD. This trades one smell for another: every consumer depends on the root config rather than on a narrow domain object.                                                                                                                                |
| B20 | **`ClubheadGenerator` produces a 6-vertex triangular prism** with a hard-coded `sole_width = 0.02`, no bounce surface, no leading-edge radius, no camber, no heel/toe relief, no grind, no lie angle, no CG/inertia. It is not a wedge. Mesh is not verified watertight/manifold; STL export computes normals per-face with no consistency check. |
| B21 | **No units discipline.** Bare floats throughout; `loft_deg`/`bounce_deg` vs `loft_rad` conversions done ad hoc.                                                                                                                                                                                                                                   |
| B22 | **No design-space tooling**: no DOE, no sweep, no sensitivity analysis, no surrogate, no optimisation. A designer cannot ask "how does this shape compare to that one?"                                                                                                                                                                           |
| B23 | **No V&V**: no verification cases, no validation data, no convergence study, no conservation checks, no credibility statement. `docs/comparison.md` asserts backend differences that are not tested.                                                                                                                                              |
| B24 | **No metrics layer.** Nothing computes the quantities a wedge designer needs (dig vs skid, entry depth, energy to sand, head deceleration, sensitivity to entry point).                                                                                                                                                                           |
| B25 | **`filter()` on `WrenchTrace` assumes uniform sampling** (`np.mean(np.diff(time))`) with no check, and `filtfilt` needs `len > 3*order`; short traces raise an opaque scipy error.                                                                                                                                                                |
| B26 | **`GrainPopulationConfig` allows `count` up to any value** with no memory/feasibility precondition; `count: 50000` in the canonical config vs `min(1000, ...)` silently applied in the MuJoCo driver — the configured value is a lie for that backend.                                                                                            |
| B27 | Both `notebooks/bunkershot3d/phase1_mvp.py` and `src/tools/bunker_shot_gui/gui.py` are separate entry points to the same thing; two launcher tiles (`bunkershot3d`, `bunker_shot`) point at different files.                                                                                                                                      |
| B28 | **The GUI never imports `bunkershot3d`.** `_run_simulation` draws `np.random.normal` particles and reports `0.5 * 0.3 * v**2` as "Est. Force". It is labelled a preview, which is honest, but it means the only user-facing surface is disconnected from the model. `src/tools/bunker_shot_gui/gui.py:141-205`                                    |

## Baseline test failures

1. `test_calibration_honesty_7999.py::TestCalibrateAllProvenance::test_non_mock_run_fails_instead_of_fabricating` — B7 (dual-module identity split).
2. `test_mpm_optional_dependency.py::test_mpm_driver_import_survives_mujoco_dll_initialization_failure` — import-guard test; `mujoco` 3.9.0 imports fine here, the test simulates `OSError(1114)` and the guard only catches `ImportError`.
