# engines.physics_engines.pinocchiothon.dtack.utils.urdf_exporter

URDF exporter from canonical YAML specification.

## Classes

### URDFExporter

Export URDF from canonical YAML model specification.

#### Methods

##### export
```python
def export(self: Any, output_path: Any) -> None
```

Export URDF file.

Args:
    output_path: Path to output URDF file

## Constants

- `MAX_EFFORT_NM`
- `MAX_VELOCITY_RAD_S`
- `MIN_UNIVERSAL_DOFS`
- `MIN_GIMBAL_DOFS`
- `JOINT_LIMIT_COUNT`
