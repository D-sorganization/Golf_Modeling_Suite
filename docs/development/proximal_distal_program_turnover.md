# Proximal–Distal Program Turnover

Updated: 2026-08-30. This is a current-state operational handoff, not a
changelog. Git, protected pull requests, issue evidence, release manifests, and
qualified artifacts remain the authorities.

## Program Authority

- Canonical completion epic: UpstreamDrift #8557.
- Active forward-attribution study: UpstreamDrift #9153.
- UpstreamDrift owns computational models, claims, evidence, registrations,
  campaign records, and the qualified technical paper.
- Tools owns reusable variation, sensitivity, and shared mechanics providers.
- AffineDrift owns immutable explanatory and publication projections; it must
  not project unqualified UpstreamDrift evidence.
- Synthetic or manufactured evidence may qualify software and numerical
  operators. It cannot replace governed participant validation.

## Protected UpstreamDrift Authority

The important research sequence is already on protected `main`; there is no
large unmerged pull request containing the last ten days of work:

1. #9152 qualified the articulated same-state drift/contact decomposition.
2. #9299 corrected and requalified Pinocchio CRBA symmetry on the research
   path.
3. #9302 qualified manufactured evidence against pinned dependency versions.
4. #9305 added the event-aligned forward-attribution kernel.
5. #9306 added signed-gap event retention, bracketed opening/reattachment
   location, duplicate-time alignment, and distributed replay.
6. #9308 adds only the prospective six-case distributed-contact smoke
   registration. It protected-squash-merged as
   `651de90a4cf8e1195ec7f3ab3ae16883ec8f6172`; reviewed head
   `142662fb3de86fbb8086b83772df09425897f9c2` and the squash share exact tree
   `a0dd17ed057c0aea45f112172d406ace971610df`. Remote `main` equals the squash.

The #9308 source diff is eight files, +703/-71. It is not a bundled feature
backlog. It contains registration and governance only, with no smoke outcome.

## Protected Turnover Evidence

- PR #9309, `Document Protected Proximal–Distal Program Turnover`, changed only
  the root and detailed handoffs.
- It protected-squash-merged as
  `3503674a90e3ca6d75e81f084f011299f5e95794`; reviewed head
  `3c99bfaef8c8d333adaa7c7604d26546a085e806` and the squash share exact tree
  `827608120154ecdfb6dc8b9f0c53b988a0454343`. Remote `main` equals the squash.
- Any post-merge turnover correction PR is recorded in the latest #9153 issue
  comment. Require its exact-head protected checks and merge evidence before
  runner implementation; preserve existing runs and auto-merge.
- After auto-merge, verify the squash SHA, remote-main ancestry, and exact tree
  equality before starting runner implementation.

## Paper and Release Authority

- Qualified paper:
  `docs/research/proximal_distal_energy_transfer/proximal_distal_energy_transfer.pdf`.
- Current qualified identity: 253 pages, 2,011,818 bytes, SHA-256
  `554fca211786ac5a06959f41b9f7d75720c89155168faeaac9d648524e8c9e36`.
- Current computational release validation covers 713 artifacts, 328 claims,
  498/498 numeric literals, 2,501 evidence references, 421 local artifacts,
  78 external URLs, and visual rendering of all 253 pages.
- The computational profile is qualified. Archival qualification remains open
  because tagged-PDF and embedded-font requirements are not yet satisfied.
- Public PDF:
  <https://raw.githubusercontent.com/D-sorganization/UpstreamDrift/main/docs/research/proximal_distal_energy_transfer/proximal_distal_energy_transfer.pdf>.

## Registered Smoke Contract

The frozen protocol is
`docs/research/proximal_distal_energy_transfer/data/articulated_distributed_smoke_registration.json`.
It binds the exact evaluator revision/tree, seven evaluator-source hashes, and
`subject_scaled_closed_contact.npz` at 35,568 bytes and SHA-256
`9fa4364571ba5535995c63226289c0711ee1ebf37c58b7a3b4e4d14a98561779`.

- Six cases: MuJoCo and Pinocchio at 1.0, 0.5, and 0.25 ms.
- Source state: case 0, sample 6, zero generalized velocity.
- Contact setup: one station per hand, 1.5 mm slack, 1 mm initial club
  displacement, and -0.8 m/s initial club velocity.
- Horizon: 50 ms; one worker; numerical thread limits of one.
- Persistence: atomic per-case checkpoints and typed failures.
- Eligible events: opening and reattachment only.
- Prohibited promotions: friction-limit entry/exit, static stick/slip,
  compliant-transition-as-impact, causal counterfactual, anatomy, biological
  passivity, human strategy, and coaching.
- Same-trajectory attribution is descriptive. A divergent intervention or
  ablation requires a separate prospective registration.

## Next Dependency-Ordered Work

1. Shepherd the latest turnover-correction PR recorded on #9153 without
   bypassing or duplicating its protected checks.
2. From that exact protected base, implement a single-worker runner that reads
   the frozen registration without rewriting it.
3. Write RED contracts first for exact protocol identity, complete six-case
   enumeration, atomic checkpoint/resume behavior, typed failure retention,
   thread limits, source/input digest rejection, and no outcome promotion.
4. Execute the six cases once only after the runner and tests are protected.
   Retain every success, null, mismatch, and numerical/event failure.
5. Add an outcome qualifier that checks completeness, cross-resolution event
   consistency, native-engine tolerances, work/impulse balance, and common
   support without relaxing thresholds after inspection.
6. Register any divergent counterfactual separately before execution.
7. Integrate only qualified results into claims, figures, the paper, release
   manifests, and then an immutable AffineDrift projection.

## Validation Commands

Use `C:\Users\diete\AppData\Local\Programs\Python\Python312\python.exe` and
serial pytest (`-n 0`) on DeskComputer.

```powershell
python -m pytest -n 0 -q tests/research/test_articulated_distributed_smoke_registration.py
python -m pytest -n 0 -q tests/research/test_articulated_contact_events.py
python -m pytest -n 0 -q tests/research/test_articulated_forward_attribution.py
python -m scripts.research.proximal_distal_energy.articulated_distributed_smoke_registration validate
python -m scripts.research.proximal_distal_energy.claim_audit validate
python -m scripts.research.proximal_distal_energy.claim_evidence_integrity validate
python -m scripts.research.proximal_distal_energy.qualify_open_release validate --source-revision (git rev-parse HEAD) --publication-profile computational
python scripts/check_document_title_case.py --changed-from origin/main
python scripts/ci/check_file_size_budget.py
python scripts/ci/check_architecture_budget.py
```

Also run Ruff, Ruff format, affected MyPy, document governance, PDF inspection,
pre-commit, serial pre-push, and protected exact-head CI before promotion.

## Frozen Recovery Boundary

UpstreamDrift #8800 is a separate interrupted uncertainty campaign, not an
input to #9153:

- Frozen source: `1bd4d57da7bd257b76b42b3cc19524b283b5f748`.
- Frozen plan contract:
  `c5cfba35ecafa96054ef8cc872f2e91a9f7855db0b93cfa491f9b18ee3db80f4`.
- Retained checkpoints: 93/830 total; shaft 48/48 and ground 45/48 for the
  completed nominal subset.
- ControlTower VHDX: `F:\WSL\ControlTower-SSD\ext4.vhdx`, unreadable with
  Windows/WSL error `0x80070570`.
- Do not retry WSL startup, run CHKDSK, repair, mount, copy, mutate, recreate,
  restart services, or launch a replacement campaign without explicit user
  approval and a written recoverability plan.
- The checkpoints are restart evidence, not a completed dataset or scientific
  result.

## Quarantined Accumulated Branch

`UpstreamDrift-worktrees/9153-forward-impulse-work` was last audited at 233
commits ahead and 48 behind its comparison base. It is not an open completion
PR and must not be merged or cherry-picked wholesale. It mixes accumulated
experiments and stale-base work. Preserve it for forensic source/evidence
comparison only; reimplement justified pieces from protected `main` with RED
tests and current registrations.

## Cross-Repository Boundaries

- Tools #4873 merged the force-source frame as
  `cc883cbaf63157b58c71cba385a683df2762b0cb`.
- Tools #4142 remains the broad reusable variation/sensitivity completion
  authority. Do not duplicate its algorithms in UpstreamDrift.
- AffineDrift #3993 pins the qualified #9152 authority as
  `9b9cbcc2199f1fbf8cd281beb08c57d543b552b1`; #3995 corrected its handoff as
  `6cc909273d63147392b17078a35c6c4da034e1da`.
- Later AffineDrift website/atlas work is presentation authority, not permission
  to import an unqualified #9153 outcome.
- Human validation gates #8556 and #9004 remain open. No qualifying governed
  participant dataset with synchronized bilateral six-axis grip wrenches is
  available.

## Local Checkout Safety

Observed at the turnover cut; recheck before acting:

- `C:\Users\diete\Repositories\UpstreamDrift` is occupied on
  `fix/9120-ci-conformance-docker-provenance`, with modified model submodules
  and an untracked `book/` tree. Do not clean, switch, or reuse it.
- `C:\Users\diete\Repositories\Tools` is occupied on
  `feat/4740-plot-export-metadata-and-inventory`, seven commits ahead and 35
  behind its local `origin/main` observation. Do not rebase, reset, or reuse it.
- `C:\Users\diete\Repositories\AffineDrift` was clean on `main` but one commit
  behind its local `origin/main` observation. Fetch through the GitHub App
  setup and use an isolated worktree for any new AffineDrift slice.
- Safe UpstreamDrift turnover checkpoint:
  `C:\Users\diete\Repositories\UpstreamDrift-worktrees\9153-turnover` on
  `docs/9153-turnover`.

## Scientific Interpretation Guardrails

- Keep energy transfer, momentum redistribution, joint work, constraint-force
  attribution, contact power, event timing, and clubhead speed distinct.
- A large force need not perform positive work; negative torque need not imply
  negative power; zero commanded torque need not imply biological passivity.
- Native-engine agreement tests declared operators and the shared physical
  contract. It does not calibrate anatomy, grip, shaft, ground, equipment, or
  a golfer.
- Retain null, adverse, contradictory, unmatched, and failed cases. Never
  change the estimand or tolerance after seeing outcomes.
- Do not issue universal human or coaching recommendations from the structural
  model ladder.

## Takeover Checklist

- Read `AGENTS.md`, `CLAUDE.md`, root `AGENT_HANDOFF.md`, #8557, and #9153.
- Run the GitHub App setup immediately before every GitHub operation.
- Verify exact remote state; do not trust a local branch name or this document
  as live merge evidence.
- Use clean leased worktrees and preserve unrelated dirty primary checkouts.
- Never force-push, bypass protection, create capacity-only reruns, or close an
  issue without merged acceptance evidence.
- Update this document and root `AGENT_HANDOFF.md` whenever the protected
  authority or next dependency changes.
