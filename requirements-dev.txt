# Development and testing dependencies for UpstreamDrift
# Install with: pip install -r requirements-dev.txt
# Or via pyproject.toml extras: pip install -e ".[dev]"

# ── Testing ──────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-cov>=4.0.0
pytest-mock>=3.0.0
pytest-benchmark>=4.0.0
pytest-timeout>=2.0.0
pytest-xdist>=3.0.0
pytest-anyio>=0.0.0; python_version>='3.10'

# ── Linting & type-checking ───────────────────────────────────────────────────
ruff>=0.1.0
mypy>=1.8.0

# ── Core runtime (required for tests) ────────────────────────────────────────
numpy>=1.24.0
fastapi>=0.110.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
httpx>=0.27.0
uvicorn>=0.27.0

# ── Database / auth ───────────────────────────────────────────────────────────
sqlalchemy>=2.0.0
bcrypt>=4.1.0
PyJWT>=2.10.1
cryptography>=44.0.0

# ── Data handling ─────────────────────────────────────────────────────────────
pandas>=2.0.0
matplotlib>=3.8.0
PyYAML>=6.0
defusedxml>=0.7.0

# ── Async support ─────────────────────────────────────────────────────────────
anyio>=4.0.0

# ── Misc ─────────────────────────────────────────────────────────────────────
email-validator>=2.0.0
build

# ── Optional: physics engines ─────────────────────────────────────────────────
# Uncomment to enable optional engine backends:
# pinocchio>=2.6.0
# meshcat>=0.3.0
# drake>=1.22.0
# opencv-python>=4.8.0
# scikit-learn>=1.3.0
