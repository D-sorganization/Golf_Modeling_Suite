---
id: GH2037
story: Fix broken pre-push hook (uses pytest instead of python3 -m pytest)
verdict: PASS
updated_at: 2026-03-20
---

# GH2037 QA Report

## Verdict: PASS

## Acceptance Criteria Review

| AC                          | Status | Notes                                                                 |
| --------------------------- | ------ | --------------------------------------------------------------------- |
| AC1: Issue resolved         | PASS   | Hook installed at .git/hooks/pre-push. Config uses python3 -m pytest. |
| AC2: All new code has tests | PASS   | No new Python production code introduced.                             |
| AC3: ruff check passes      | PASS   | `ruff check .` → "All checks passed!"                                 |
| AC4: No new print() in src/ | PASS   | No src/ files modified.                                               |
| AC5: PR targeting staging   | PASS   | PR created.                                                           |

## QA Checks

- `python3 -m ruff check .` → All checks passed!
- `python3 -m ruff format --check .` → 1937 files already formatted
- Pre-push hook: `.git/hooks/pre-push` exists and is executable
- Hook config: `python3 -m pytest tests/unit ...` confirmed in `.pre-commit-config.yaml:135`

## Pre-existing Test Failures (Not Introduced)

The following test failures exist on `staging` prior to this story and are unrelated:

- `tests/unit/dashboard/test_widgets.py::test_live_plot_widget_init` — file import error
- `tests/unit/shared_python/test_plotting.py::test_plot_club_head_trajectory` — matplotlib Axes3D unavailable

## Summary

All acceptance criteria pass. The pre-push hook is now installed and correctly uses
`python3 -m pytest`. Documentation is updated to reflect the fix. No scope changes required.
