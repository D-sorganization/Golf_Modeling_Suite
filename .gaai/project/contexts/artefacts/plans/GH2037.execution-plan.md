---
id: GH2037
story: Fix broken pre-push hook (uses pytest instead of python3 -m pytest)
tier: 2
complexity: 1
updated_at: 2026-03-20
---

# GH2037 Execution Plan

## Problem Statement

The pre-push hook was documented as broken (using bare `pytest` instead of `python3 -m pytest`).
Investigation shows:

1. The `.pre-commit-config.yaml` was already fixed in commit `701edb0f` to use `python3 -m pytest`
2. No `.git/hooks/pre-push` is installed — `pre-commit install --hook-type pre-push` hasn't been run
3. `CLAUDE.md` and `project.rules.md` both still contain stale workaround notes

## Approach

Fix the hook by installing it properly and removing stale documentation warnings.

### Step 1: Install the pre-push hook

Run `pre-commit install --hook-type pre-push` in the worktree to install the hook
from the already-correct `.pre-commit-config.yaml`.

### Step 2: Update CLAUDE.md

Remove the "Pre-push hook is broken" note from the Known Constraints section since
the hook is now fixed.

### Step 3: Update project.rules.md

Remove rule 12 ("Pre-push hook is broken...") and renumber remaining rules.

## Acceptance Criteria Mapping

- AC1: Issue resolved — install the pre-push hook using the already-correct config
- AC2: Tests — no new code introduced; hook script is external tooling
- AC3: ruff check — no Python files modified
- AC4: No new print() in src/ — no src/ files modified
- AC5: PR targeting staging — create PR at end

## Files Changed

1. `CLAUDE.md` — remove stale Known Constraints note
2. `.gaai/project/contexts/rules/project.rules.md` — remove stale rule 12
3. `.git/hooks/pre-push` (installed by pre-commit, not checked in)
