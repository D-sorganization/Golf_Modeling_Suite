---
id: GH2037
story: Fix broken pre-push hook (uses pytest instead of python3 -m pytest)
status: complete
updated_at: 2026-03-20
---

# GH2037 Implementation Report

## Summary

Fixed the broken pre-push hook by installing it properly via `pre-commit install --hook-type pre-push`. Updated `scripts/setup_precommit.sh` to install both commit-stage and pre-push hooks, use `python3` consistently, and removed unused `black`/`isort` dependencies. Removed stale workaround documentation from `CLAUDE.md` and `project.rules.md`.

## Root Cause Analysis

The `.pre-commit-config.yaml` `pytest-unit` hook was already corrected to use `python3 -m pytest` in commit `701edb0f` (#1404). However:

1. `pre-commit install --hook-type pre-push` was never run after that fix
2. `scripts/setup_precommit.sh` only called `pre-commit install` (commit-stage only), not `pre-commit install --hook-type pre-push`
3. Documentation in `CLAUDE.md` and `project.rules.md` still listed the hook as broken

## Files Changed

| File                                            | Change                                                        |
| ----------------------------------------------- | ------------------------------------------------------------- |
| `scripts/setup_precommit.sh`                    | Use `python3`, install pre-push hook, remove unused deps      |
| `CLAUDE.md`                                     | Remove stale "Pre-push hook is broken" Known Constraints note |
| `.gaai/project/contexts/rules/project.rules.md` | Update rule 12 to document fix + onboarding step              |

## Acceptance Criteria

- AC1: Issue resolved — hook installed, uses python3 -m pytest from .pre-commit-config.yaml
- AC2: No new production code introduced — no tests needed
- AC3: ruff check passes — no Python files modified
- AC4: No new print() in src/ — no src/ files modified
- AC5: PR created targeting staging
