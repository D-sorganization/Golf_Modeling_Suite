name: Test Runner Env

on:
  push:
    branches: [ fix/toolcache-shared-race ]
  workflow_dispatch:

jobs:
  inspect:
    runs-on: d-sorg-fleet-docker
    steps:
      - name: Inspect Runner Hooks
        run: |
          echo "=== job-started.sh ==="
          cat /usr/local/bin/runner-hooks/job-started.sh || true
          echo "=== runner-post-job.sh ==="
          cat /home/dieterolson/actions-runners/fleet/runner-post-job.sh || true
