name: Test Runner Env

on:
  push:
    branches: [ fix/toolcache-shared-race ]
  workflow_dispatch:

jobs:
  inspect:
    runs-on: d-sorg-fleet-docker
    steps:
      - name: List Python Version Cache Files
        run: |
          echo "=== List Python/3.11.15 ==="
          ls -la /home/dieterolson/actions-runners-nvme/.shared-tool-cache/Python/3.11.15/ || true
          echo "=== List Python/3.11.15/x64 ==="
          ls -la /home/dieterolson/actions-runners-nvme/.shared-tool-cache/Python/3.11.15/x64/ || true
